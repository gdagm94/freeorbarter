# Copy to `.env` and adjust values for local development.
# These values are read at build time via Vite's `import.meta.env`.

VITE_SUPABASE_URL=https://xvdltodlekapbklymsvz.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Optional override. Defaults to the site's origin + `/reset-password`.
VITE_PASSWORD_RESET_REDIRECT=https://freeorbarter.com/reset-password

# Other build-time environment variables already consumed by the app.
VITE_PUSHER_APP_KEY=
VITE_PUSHER_CLUSTER=
VITE_APP_VERSION=local

